application:
  jwt:
    secretKey: LfrY1W9fI3dTVi7JjXnPPOMts4Czswhe
    tokenExpirationAfterDays: 14
    tokenPrefix: 'Bearer '
server:
  error:
    include-binding-errors: always
    include-message: always
spring:
  datasource:
    password: ''
    url: jdbc:postgresql://localhost:5432/library
    username: ''
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  mail:
    host: localhost
    password: hello
    port: 1025
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 5000
          ssl:
            trust: '*'
          starttls:
            enabled: true
          timeout: 3000
          writetimeout: 5000
    username: hello
